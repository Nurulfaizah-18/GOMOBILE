name: rental_kendaraan
description: "GOMOBILE - Aplikasi Rental Kendaraan Modern"
publish_to: 'none'

version: 1.0.4+4

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management - Riverpod (Recommended) 
  # Uncomment one option below:
  
  # Option 1: Riverpod (Recommended for this project)
  flutter_riverpod: ^2.4.0
  riverpod: ^2.4.0
  
  # Option 2: Provider (Simpler alternative)
  # provider: ^6.0.0

  # Navigation & UI
  google_nav_bar: ^5.0.5
  flutter_svg: ^2.0.5

  # Date & Time
  intl: ^0.19.0

  # Local Storage
  shared_preferences: ^2.2.0

  # Networking
  dio: ^5.3.0

  # Model Serialization
  json_annotation: ^4.8.0

  # Image Picker
  image_picker: ^1.0.0

  # Utility
  equatable: ^2.0.5
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  json_serializable: ^6.7.0

flutter:
  uses-material-design: true

  # Assets configuration (uncomment once files are added)
  # assets:
  #   - assets/images/
  #   - assets/icons/

  # Fonts configuration (uncomment once files are added)
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700
  #       - asset: assets/fonts/Poppins-SemiBold.ttf
  #         weight: 600
